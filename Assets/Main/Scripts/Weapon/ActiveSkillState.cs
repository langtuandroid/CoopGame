namespace Main.Scripts.Weapon
{
    public enum ActiveSkillState
    {
        NotAttacking,
        Attacking,
        WaitingForPoint,
        WaitingForTarget,
        Finished,
        Canceled,
    }
}